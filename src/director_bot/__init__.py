"""Director-bot: embodied AI film director.

Packages:
  contracts  — shared schemas (scene cards, works, decisions)
  canon      — S-tier corpus DB + query
  soul       — WorkingMemory, MentalProcesses, static personality
  decisions  — merkle ledger + multi-criteria equilibrium
  adapters   — thin hooks to scripty / lightwriter / script2screen
"""

__version__ = "0.3.0"
